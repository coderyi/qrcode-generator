import { DEFAULT_VALUE } from '@/constants';

const state = {
  qrValue: DEFAULT_VALUE,
  qrLightColor: '#ffffff',
  qrDarkColor: '#000000',
  fileType: 'png',
  qrCodeContentIndex: 1
};

export default state;
